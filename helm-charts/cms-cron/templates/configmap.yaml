apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cms-cron.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "cms-cron.labels" . | nindent 4 }}
    app.kubernetes.io/component: cron
data:
  {{- toYaml .Values.envData | nindent 4 }}
